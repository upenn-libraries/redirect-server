# Whitelist of redirect target hosts, including entire
# URI before the path porition. Default ports are not 
# normalized, so list them separately to allow redirects
# with explicit ports

#http://allow.redirect.from.com
#http://localhost:8080
